[build]
  command = "npm run build"
  publish = "dist"

# Redireciona todas as rotas para o index.html (necessário para React Router)
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
